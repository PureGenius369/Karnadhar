# Copy to .env (git-ignored). Both keys are OPTIONAL — the engine runs fully
# without them (labelled fallbacks / template briefs).
AISSTREAM_API_KEY=        # free key from aisstream.io — live AIS vessel feed
ANTHROPIC_API_KEY=        # optional: Claude writes brief prose (never numbers)
